 #include <bits/stdc++.h>
using namespace std;

int main(){
		long long int y=0,n;
		cin>>n;
		long long int x[n];
		for (int i = 0; i < n; ++i)
		{
			cin >> x[i];
		}
		sort(x,x+n);
		for (int i = 0; i < n; ++i)
		{
			y+=abs(x[n/2]-x[i]);	
		}
	
	cout<<y;
}
